const User = require('./user');

/**
 * @description register the models here is just a flavor
 * you don't have to do it but if you did it it will
 * make importing the models easier and cleaner
 * especially when you're using more than one model
 * in a single file
 */

module.exports = {
  User,
};
